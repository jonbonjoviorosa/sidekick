@model Sidekick.Model.Specialty.Specialty;

<div class="modal fade"
     id="deleteSpecialtyCategoryModal"
     tabindex="-1"
     aria-labelledby="deleteSpecialtyCategoryModalLabel"
     aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header border-0">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-controller="Setup" asp-action="DeleteSpecialty" method="post">
                <div class="modal-body px-5 py-0">
                    <div class="text-center">
                        <h5 class="modal-title mb-3" id="appendDeleteSpecialtyText"></h5>
                        <input type="text" id="delete-specialty-id" asp-for="SpecialtyId" hidden />
                    </div>
                </div>
                <div class="modal-footer border-0 text-center">
                    <input type="submit" value="Delete" class="btn login-button btn-padding" id="ok-location-btn" />
                </div>
            </form>
        </div>
    </div>
</div>